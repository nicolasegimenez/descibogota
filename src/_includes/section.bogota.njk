{% for item in collections.sections %}
  {% if item.data.type === 'bogota' %}
    <div id="bogota" class="w-full min-h-screen flex flex-col justify-center items-center bg-green-600 py-12">
      <div class="self-center text-center w-full sm:w-2/3 xl:w-1/2 px-4 sm:px:0">
        <h2 class="tracking-wide text-blue-200 font-bold text-4xl mb-4">{{ item.data.title }}</h2>
        <p class="font-light text-gray-200 font-bold text-xl mb-6">
          {{ item.templateContent | safe }}
        </p>
      </div>
    </div>
  {% endif %}
{% endfor %}
